services:
  khb_onlinesheet:
    image: nginx:1.27-alpine
    container_name: khb_onlinesheet
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
